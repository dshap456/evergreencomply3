import { fetchDataFromSupabase as c } from "@makerkit/data-loader-supabase-core";
const o = 10;
async function r(e) {
  const t = await c(e), a = e.limit ?? o, n = Math.ceil(t.count / a);
  return e.children({
    ...t,
    page: e.page ?? 1,
    pageCount: n,
    pageSize: a
  });
}
export {
  r as ServerDataLoader
};
